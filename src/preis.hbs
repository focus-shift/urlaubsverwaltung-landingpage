---
layout: layout-subpage
backgroundColor: bg-sky-50
navigation:
  activeItem: preis
stylesheets:
  - /static/css/faq.css
title: Preis der urlaubsverwaltung.cloud
description: Open Source, on Premise oder in der Cloud für den besten Preis
---

<div class="mb-16 md:mb-24">
	{{> partials/price }}
</div>

		<div>
			<h2 class="mt-8 text-2xl font-bold mb-6" id="live-demo">
				Du hast noch letzt Fragen? Vereinbare einfach einen Termin.
			</h2>

<!-- Cal inline embed code begins -->
<div style="width:100%;height:100%;overflow:scroll" id="my-cal-inline"></div>
<script type="text/javascript">
  (function (C, A, L) { let p = function (a, ar) { a.q.push(ar); }; let d = C.document; C.Cal = C.Cal || function () { let cal = C.Cal; let ar = arguments; if (!cal.loaded) { cal.ns = {}; cal.q = cal.q || []; d.head.appendChild(d.createElement("script")).src = A; cal.loaded = true; } if (ar[0] === L) { const api = function () { p(api, arguments); }; const namespace = ar[1]; api.q = api.q || []; typeof namespace === "string" ? (cal.ns[namespace] = api) && p(api, ar) : p(cal, ar); return; } p(cal, ar); }; })(window, "https://app.cal.com/embed/embed.js", "init");
Cal("init",  {origin:"https://cal.com"});

  Cal("inline", {
	elementOrSelector:"#my-cal-inline",
	calLink: "aweigel/offene-fragen",
	layout: "month_view"
  });

  Cal("ui", {"styles":{"branding":{"brandColor":"#f0f9ff"}},"hideEventTypeDetails":false,"layout":"month_view"});
  </script>
  <!-- Cal inline embed code ends -->
		</div>
	</div>

{{> partials/faq category="price" }}
